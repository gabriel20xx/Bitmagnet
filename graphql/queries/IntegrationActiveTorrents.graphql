query IntegrationActiveTorrents($id: ID!, $input: IntegrationActiveTorrentsQueryInput) {
  integrationActiveTorrents(id: $id, input: $input) {
    totalCount
    items {
      hash
      name
      progress
      state
      downloadSpeed
      eta
      size
    }
  }
}
